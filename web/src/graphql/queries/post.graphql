query Post ($id: Int!) {
    post(id: $id) {
        id
        createdAt
        updatedAt
        title
        points
        audioFileName
        imageFileName
        text
        voteStatus
        creator {
            id
            username
        }
    }
}