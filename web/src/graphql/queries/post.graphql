query Post ($id: Int!) {
    post(id: $id) {
        id
        createdAt
        updatedAt
        title
        points
        fileName
        text
        voteStatus
        creator {
            id
            username
        }
    }
}