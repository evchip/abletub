query Post ($id: Int!) {
    post(id: $id) {
        id
        createdAt
        updatedAt
        title
        points
        text
        fileName
        voteStatus
        creator {
            id
            username
        }
    }
}